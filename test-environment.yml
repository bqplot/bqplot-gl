name: bqplot-gl-test
channels:
    - conda-forge
dependencies:
    - pip
    - python=3.11
    - build
    - yarn
    - ipywidgets
    - traitlets
    - traittypes
    - numpy
    - pandas
    - bqscales>=0.3.2
    - scipy
    - notebook
    - jupyterlab=4  # to build the lab federated bundle
    - jupyter-packaging # to build the wheel
    - pytest
    - nbval
    - pytest-cov
    - selenium
    - flake8
    - nose
